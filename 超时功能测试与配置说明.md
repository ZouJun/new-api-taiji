# 超时功能测试与配置说明

本文档整理当前项目中“渠道超时拦断”相关的测试点、配置取值逻辑、错误抛出方式、日志表现和重试行为，便于后续排查、联调和交接。

## 1. 本次已完成的测试点

### 1.1 手工联调验证过的测试点

#### 1. 非流式正常请求
- 模型：`mock-gpt`
- 目的：确认基础链路正常
- 结果：返回 `200`

#### 2. 非流式超时拦断
- 模型：`mock-nonstream-delay-5s`
- 渠道配置：`non_stream_timeout_seconds = 3`
- 验证点：
  - 超时后 `new-api` 是否返回 `504`
  - 错误是否被统一封装
  - 上游连接是否被主动取消
- 结果：
  - `new-api` 返回 `504`
  - 上游日志出现 `http connection closed by client`
  - 说明：非流式超时后，客户端会主动取消上游 HTTP 连接

#### 3. 流式首包超时拦断
- 模型：`mock-stream-first-byte-10s`
- 渠道配置：`stream_first_byte_timeout_seconds = 5`
- 验证点：
  - 流式请求是否按“首包超时”判定
  - 超时后 `new-api` 是否返回 `504`
  - 上游连接是否被主动取消
- 结果：
  - `new-api` 返回 `504`
  - 上游日志出现 `http connection closed by client`
  - 说明：流式首包超时后，也会主动取消上游 HTTP 连接

#### 4. 流式慢流但首包正常
- 模型：`mock-stream-chunk-interval-2s`
- 目的：确认流式不会因为整条流耗时长而被误杀
- 验证点：
  - 首包及时返回后，后续慢流是否还能继续
- 结果：
  - 返回 `200`
  - 流式输出正常结束
  - 说明：当前 v1 方案只控制“首包等待”，不控制整条流总耗时

#### 5. 上游直接返回 504
- 模型：`mock-status-504`
- 目的：区分“本地超时拦断”和“上游主动报错”
- 结果：
  - 返回 `504`
  - 但这不是本地超时，而是上游自己直接返回了 `504`

#### 6. 错误日志落库验证
- 已确认 `logs.other` 可记录：
  - `timeout_type`
  - `timeout_source`
  - `timeout_seconds`
  - `timeout_provider`
  - `timeout_stage`
  - `retry_index`
  - `is_stream`

### 1.2 自动化测试已覆盖的测试点

测试文件：
- [relay/channel/api_request_test.go](/Users/zf/Project/new-api/relay/channel/api_request_test.go)

#### 1. 非流式超时自动化测试
- 用例：`TestDoRequest_NonStreamTimeoutCancelsUpstreamConnection`
- 验证点：
  - `channel.setting.non_stream_timeout_seconds` 生效
  - 请求会在超时后快速返回，不会长时间等待上游
  - 返回状态码为 `504`
  - 错误码为 `do_request_failed`
  - timeout meta 为：
    - `timeout_type = non_stream_total`
    - `timeout_source = channel_setting`
    - `timeout_stage = http_request`
  - 上游 TCP 连接会被客户端主动断开

#### 2. 流式首包超时自动化测试
- 用例：`TestDoRequest_StreamFirstByteTimeoutCancelsUpstreamConnection`
- 验证点：
  - `channel.setting.stream_first_byte_timeout_seconds` 生效
  - 超时后快速返回
  - 返回状态码为 `504`
  - 错误码为 `do_request_failed`
  - timeout meta 为：
    - `timeout_type = stream_first_byte`
    - `timeout_source = channel_setting`
    - `timeout_stage = stream_first_byte_wait`
  - 上游 TCP 连接会被客户端主动断开

测试命令：

```bash
GOROOT=/Users/zf/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.7.darwin-amd64 \
PATH=/Users/zf/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.7.darwin-amd64/bin:$PATH \
/Users/zf/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.7.darwin-amd64/bin/go test ./relay/channel -run 'TestDoRequest_(NonStreamTimeout|StreamFirstByteTimeout)' -count=1 -v
```

### 1.3 `/v1` 路由覆盖测试

测试文件：
- [router/v1_routes_test.go](/Users/zf/Project/new-api/router/v1_routes_test.go)

已覆盖：

#### 1. `/v1*` 路由注册清单校验
- 断言 `/v1`、`/v1beta`、`/v1/dashboard`、`/v1/video*` 路由均已注册

#### 2. `/v1*` 路由 smoke 测试
- 逐路由发起请求
- 确认不会落到 `404` / `405`

测试命令：

```bash
GOROOT=/Users/zf/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.7.darwin-amd64 \
PATH=/Users/zf/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.7.darwin-amd64/bin:$PATH \
/Users/zf/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.25.7.darwin-amd64/bin/go test ./router -run 'TestV1Routes' -count=1
```

## 2. 超时配置取值逻辑

### 2.1 普通渠道非流式超时

字段：
- `channel.setting.non_stream_timeout_seconds`

取值顺序见 [relay/common/timeout.go](/Users/zf/Project/new-api/relay/common/timeout.go:48)：

1. `channel.setting.non_stream_timeout_seconds`
2. `RELAY_DEFAULT_NON_STREAM_TIMEOUT`
3. `RELAY_TIMEOUT`
4. 都没有则返回 `0`

### 2.2 普通渠道流式首包超时

字段：
- `channel.setting.stream_first_byte_timeout_seconds`

取值顺序见 [relay/common/timeout.go](/Users/zf/Project/new-api/relay/common/timeout.go:63)：

1. `channel.setting.stream_first_byte_timeout_seconds`
2. `RELAY_DEFAULT_STREAM_FIRST_BYTE_TIMEOUT`
3. `RELAY_TIMEOUT`
4. 都没有则返回 `0`

### 2.3 AWS 非流式 Invoke 超时

字段：
- `channel.setting.aws_invoke_timeout_seconds`

取值顺序见 [relay/common/timeout.go](/Users/zf/Project/new-api/relay/common/timeout.go:89)：

1. `channel.setting.aws_invoke_timeout_seconds`
2. `AWS_INVOKE_TIMEOUT_SECONDS`
3. 回退到普通非流式超时链：
   - `RELAY_DEFAULT_NON_STREAM_TIMEOUT`
   - `RELAY_TIMEOUT`
4. 都没有则返回 `0`

### 2.4 AWS SDK 最大尝试次数

字段：
- `channel.setting.aws_sdk_max_attempts`

取值顺序见 [relay/common/timeout.go](/Users/zf/Project/new-api/relay/common/timeout.go:78)：

1. `channel.setting.aws_sdk_max_attempts`
2. `AWS_SDK_MAX_ATTEMPTS`
3. 都没有则返回 `0`

说明：

- `0` 不是“重试 0 次”，而是“不额外覆盖 AWS SDK 默认重试策略”
- 当该值大于 `0` 时，会写入 AWS Bedrock Client 的 `RetryMaxAttempts`

配置示例：

1. 只给单个 AWS 渠道单独设置：

```json
{
  "aws_invoke_timeout_seconds": 15,
  "aws_sdk_max_attempts": 4
}
```

2. 走全局默认值：

```env
AWS_INVOKE_TIMEOUT_SECONDS=15
AWS_SDK_MAX_ATTEMPTS=4
```

3. 优先级说明：

- 如果 `channel.setting.aws_sdk_max_attempts=6`
- 同时 `.env` 里 `AWS_SDK_MAX_ATTEMPTS=4`
- 最终该渠道实际生效的是 `6`

### 2.5 当前 `.env` 中的默认值

当前配置见 [.env](/Users/zf/Project/new-api/.env:58)：

```env
RELAY_TIMEOUT=0
RELAY_DEFAULT_NON_STREAM_TIMEOUT=0
RELAY_DEFAULT_STREAM_FIRST_BYTE_TIMEOUT=0
AWS_INVOKE_TIMEOUT_SECONDS=0
AWS_SDK_MAX_ATTEMPTS=0
```

结论：

- 当前项目里，如果 `channel.setting` 留空
- 默认值实际全部会回退到 `0`
- `0` 的含义不是“立即超时”，而是“不启用这层超时”

## 3. 超时错误是如何向外层抛出的

核心代码在 [relay/channel/api_request.go](/Users/zf/Project/new-api/relay/channel/api_request.go:487) 的 `doRequest(...)`。

### 3.1 非流式

非流式请求会同时启用两层保护：

1. `context.WithTimeout(...)`
2. `http.Client.Timeout`

相关代码：
- [relay/channel/api_request.go](/Users/zf/Project/new-api/relay/channel/api_request.go:516)
- [service/http_client.go](/Users/zf/Project/new-api/service/http_client.go:58)

### 3.2 流式

流式请求当前只控制“首包等待”：

1. 通过 `NewFirstByteTimeoutContext(...)` 控制首包等待时间
2. 首包到达后停止计时
3. 不再按整条流总时长硬切断

### 3.3 底层超时后如何封装

当 `client.Do(req)` 返回错误时，走 [relay/channel/api_request.go](/Users/zf/Project/new-api/relay/channel/api_request.go:558) 这段逻辑：

1. 打原始错误日志
2. 统一使用错误码：`do_request_failed`
3. 如果识别到是超时：
   - `context deadline exceeded`
   - `stream first byte timeout`
   则状态码改为 `504`
4. 对外错误消息统一隐藏为：
   - `upstream error: do request failed`

最终返回的是：
- `*types.NewAPIError`
- `StatusCode = 504`
- `ErrorCode = do_request_failed`

### 3.4 客户端看到的最终错误

外层在 [controller/relay.go](/Users/zf/Project/new-api/controller/relay.go:79) 统一返回：

```json
{
  "error": {
    "message": "upstream error: do request failed (request id: xxx)",
    "type": "new_api_error",
    "code": "do_request_failed"
  }
}
```

## 4. 超时日志是什么样的

### 4.1 底层原始错误日志

打印位置：
- [relay/channel/api_request.go](/Users/zf/Project/new-api/relay/channel/api_request.go:564)

打印格式：

```text
do request failed: <原始错误>
```

典型示例：

非流：

```text
[ERR] ... | requestId | do request failed: Post "http://xxx": context deadline exceeded
```

流首包：

```text
[ERR] ... | requestId | do request failed: stream first byte timeout after N seconds
```

### 4.2 外层渠道错误日志

打印位置：
- [controller/relay.go](/Users/zf/Project/new-api/controller/relay.go:345)

典型示例：

```text
[ERR] ... | requestId | channel error (channel #1, status code: 504): upstream error: do request failed
```

说明：
- 到这一层已经是“封装后的错误文案”
- 不再直接暴露 `context deadline exceeded`

### 4.3 最外层 relay error 日志

打印位置：
- [controller/relay.go](/Users/zf/Project/new-api/controller/relay.go:82)

典型示例：

```text
[ERR] ... | requestId | relay error: upstream error: do request failed
```

### 4.4 错误日志表中的内容

当 `ERROR_LOG_ENABLED=true` 时，会记录到 `logs` 表。

典型 `content`：

```text
status_code=504, upstream error: do request failed
```

典型 `other`：

```json
{
  "status_code": 504,
  "error_code": "do_request_failed",
  "timeout_type": "non_stream_total",
  "timeout_source": "channel_setting",
  "timeout_seconds": 3,
  "timeout_stage": "http_request"
}
```

流式首包超时时类似：

```json
{
  "status_code": 504,
  "error_code": "do_request_failed",
  "timeout_type": "stream_first_byte",
  "timeout_source": "channel_setting",
  "timeout_seconds": 5,
  "timeout_stage": "stream_first_byte_wait"
}
```

## 5. 超时后是否进入重试

结论：

- 会进入最外层的“是否重试”判定流程
- 但当前 **不会实际重试**

### 5.1 判定入口

最外层调用：
- [controller/relay.go](/Users/zf/Project/new-api/controller/relay.go:234)

判定函数：
- [controller/relay.go](/Users/zf/Project/new-api/controller/relay.go:325)

### 5.2 为什么不会重试

最终判定依赖：
- [setting/operation_setting/status_code_ranges.go](/Users/zf/Project/new-api/setting/operation_setting/status_code_ranges.go:80)

当前硬编码跳过重试的状态码：

```go
var alwaysSkipRetryStatusCodes = map[int]struct{}{
    504: {},
    524: {},
}
```

所以：

- 超时最终返回 `504`
- `shouldRetry(...)` 会走到状态码判断
- `ShouldRetryByStatusCode(504)` 返回 `false`
- 因此不会实际重试

对应测试也已经写明：
- [setting/operation_setting/status_code_ranges_test.go](/Users/zf/Project/new-api/setting/operation_setting/status_code_ranges_test.go:65)

## 6. AWS 特殊说明

AWS 超时和普通 HTTP relay 超时不完全一样。

### 6.1 AWS 超时返回码

状态码：
- `504`

错误码：
- `aws_invoke_error`

相关代码：
- [relay/channel/aws/relay-aws.go](/Users/zf/Project/new-api/relay/channel/aws/relay-aws.go:31)

### 6.2 AWS 非流式

AWS 非流式超时由：
- `newAwsInvokeContext(...)`

控制，相关代码：
- [relay/channel/aws/relay-aws.go](/Users/zf/Project/new-api/relay/channel/aws/relay-aws.go:45)

### 6.3 AWS 流式

AWS 流式当前只控制：
- 首包等待超时

相关代码：
- [relay/channel/aws/relay-aws.go](/Users/zf/Project/new-api/relay/channel/aws/relay-aws.go:272)

## 7. 当前结论

当前超时功能已验证清楚：

1. `channel.setting` 的超时配置可以生效
2. 非流与流式首包超时已经分离
3. 流式当前只看首包，不看整条流总耗时
4. 超时后会主动取消上游连接
5. 普通 HTTP relay 超时会统一返回：
   - `504`
   - `do_request_failed`
   - `upstream error: do request failed`
6. 超时元信息会写入错误日志
7. 当前超时错误不会进入实际重试

## 8. 相关代码位置索引

- 超时解析：
  - [relay/common/timeout.go](/Users/zf/Project/new-api/relay/common/timeout.go)
- HTTP 请求超时执行：
  - [relay/channel/api_request.go](/Users/zf/Project/new-api/relay/channel/api_request.go)
- AWS 超时执行：
  - [relay/channel/aws/relay-aws.go](/Users/zf/Project/new-api/relay/channel/aws/relay-aws.go)
- 外层错误返回和重试判定：
  - [controller/relay.go](/Users/zf/Project/new-api/controller/relay.go)
- HTTP client timeout 生效点：
  - [service/http_client.go](/Users/zf/Project/new-api/service/http_client.go)
- 当前环境变量：
  - [.env](/Users/zf/Project/new-api/.env)
