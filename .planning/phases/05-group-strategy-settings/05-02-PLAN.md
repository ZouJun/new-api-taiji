---
phase: 05-group-strategy-settings
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [controller/relay.go, service/channel_select.go, relay/common/relay_info.go, relay/common/group_strategy.go, relay/common/group_strategy_test.go, controller/relay_retry_test.go, constant/context_key.go]
autonomous: true
requirements: [GSET-04, GSET-05, GSET-06, GSET-07]
user_setup: []
must_haves:
  truths:
    - Relay retries use the current group strategy when present and global RetryTimes when absent.
    - Streaming retries accumulate first-byte wait budget across attempts and stop with configured fallback response only when no clearer upstream error should win.
  artifacts:
    - relay/common/group_strategy.go
    - relay/common/group_strategy_test.go
    - controller/relay_retry_test.go
  key_links:
    - controller/relay.go uses a shared effective retry policy resolver instead of hard-wiring common.RetryTimes everywhere
    - service/channel_select.go and controller/relay.go apply the same effective retry ceiling semantics
---

<objective>
Apply group strategy settings to the live relay path.

Purpose: Make the new per-group retry and stream budget controls actually affect request execution while preserving existing timeout and cross-group retry semantics.
Output: Shared runtime strategy helpers, retry-loop integration, stream budget accounting, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-group-strategy-settings/05-CONTEXT.md
@.planning/phases/05-group-strategy-settings/05-RESEARCH.md
@.planning/phases/05-group-strategy-settings/05-01-PLAN.md
@controller/relay.go
@service/channel_select.go
@relay/common/timeout.go
@relay/common/relay_info.go
@constant/context_key.go
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce shared runtime strategy resolution and request budget tracking</name>
  <files>relay/common/group_strategy.go, relay/common/group_strategy_test.go, relay/common/relay_info.go, constant/context_key.go</files>
  <read_first>setting/operation_setting/group_strategy.go, relay/common/timeout.go, relay/common/relay_info.go, constant/context_key.go, .planning/phases/05-group-strategy-settings/05-RESEARCH.md</read_first>
  <action>Add `relay/common/group_strategy.go` to centralize runtime logic for Phase 5. Implement helpers that resolve the current group from context/relay info, fetch the effective group strategy, compute the effective retry ceiling, and maintain one request-level accumulator for total stream first-byte wait spent across retries. Add dedicated context keys in `constant/context_key.go` for strategy snapshot and cumulative first-byte wait accounting. Extend `relay/common/relay_info.go` only where needed so the current matched group and strategy metadata can be logged or inspected without re-parsing raw option JSON repeatedly. Cover the helper behavior with tests in `relay/common/group_strategy_test.go`, especially missing-group fallback and cross-retry accumulator updates.</action>
  <verify>go test ./relay/common -run 'TestGroupStrategy|Test.*FirstByte' </verify>
  <acceptance_criteria>
    - `relay/common/group_strategy.go` contains `ResolveEffectiveRetryTimes`
    - `relay/common/group_strategy.go` contains `ConsumeStreamFirstByteRetryBudget` or an equivalently named accumulator helper
    - `constant/context_key.go` contains new group-strategy-related context keys
    - `go test ./relay/common -run 'TestGroupStrategy|Test.*FirstByte'` exits 0
  </acceptance_criteria>
  <done>Shared runtime strategy resolution and budget tracking exist with focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Apply effective retry and budget logic to relay execution</name>
  <files>controller/relay.go, service/channel_select.go, controller/relay_retry_test.go</files>
  <read_first>controller/relay.go, service/channel_select.go, controller/relay_retry_test.go, relay/common/group_strategy.go, setting/operation_setting/group_strategy.go</read_first>
  <action>Refactor the relay retry loop in `controller/relay.go` so it no longer relies only on `common.RetryTimes`. Resolve the effective retry count from the current group strategy on each retry attempt, while preserving the product rule that the first request does not count as a retry. Update the stream retry path to accumulate first-byte wait budget across attempts and synthesize the configured HTTP status and message only when that budget is exhausted and the upstream original error is not more specific. Update `service/channel_select.go` so auto-group switching uses the same effective retry ceiling logic instead of directly comparing against `common.RetryTimes`. Add or extend tests in `controller/relay_retry_test.go` for timeout retry eligibility, per-group retry override, missing-group fallback, and upstream-original-error priority.</action>
  <verify>go test ./controller ./service ./relay/common -run 'Test.*Retry|Test.*GroupStrategy'</verify>
  <acceptance_criteria>
    - `controller/relay.go` no longer has the only retry loop condition tied directly to `common.RetryTimes`
    - `service/channel_select.go` contains a call to the shared group strategy retry resolver or equivalent helper
    - `controller/relay_retry_test.go` contains coverage for group strategy retry override
    - `go test ./controller ./service ./relay/common -run 'Test.*Retry|Test.*GroupStrategy'` exits 0
  </acceptance_criteria>
  <done>Live relay execution honors per-group retry and stream first-byte budget rules without regressing existing retry semantics.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./relay/common ./controller ./service -run 'Test.*Retry|Test.*GroupStrategy'`
- [ ] Relay retry path still retries `context.DeadlineExceeded` and `ErrStreamFirstByteTimeout` where allowed
- [ ] Missing group strategy uses current global behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Current-group strategy resolution is the single source of truth for Phase 5 runtime behavior
</success_criteria>

<output>
After completion, create `.planning/phases/05-group-strategy-settings/05-02-SUMMARY.md`
</output>
