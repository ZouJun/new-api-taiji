---
phase: 05-group-strategy-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [model/option.go, controller/option.go, setting/operation_setting/group_strategy.go, setting/operation_setting/group_strategy_test.go, web/default/src/features/system-settings/types.ts]
autonomous: true
requirements: [GSET-02, GSET-03, GSET-05, GSET-06]
user_setup: []
must_haves:
  truths:
    - Group strategy data is stored in an independent option key and validated before persistence.
    - Runtime code can resolve a group strategy by current GroupRatio key with explicit fallback to existing global behavior.
  artifacts:
    - setting/operation_setting/group_strategy.go
    - setting/operation_setting/group_strategy_test.go
  key_links:
    - controller/option.go validates group_strategy_settings using the backend struct in setting/operation_setting/group_strategy.go
    - model/option.go exposes the new option key to the existing option loading pipeline
---

<objective>
Create the backend persistence and resolution foundation for group strategy settings.

Purpose: Lock the storage shape, validation rules, and fallback semantics before retry-loop changes or UI work start.
Output: A dedicated group strategy option model, option validation hook, runtime resolution helpers, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-group-strategy-settings/05-CONTEXT.md
@.planning/phases/05-group-strategy-settings/05-RESEARCH.md
@AGENTS.md
@model/option.go
@controller/option.go
@setting/ratio_setting/group_ratio.go
@web/default/src/features/system-settings/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define validated group strategy option model and fallback helpers</name>
  <files>setting/operation_setting/group_strategy.go, setting/operation_setting/group_strategy_test.go</files>
  <read_first>setting/ratio_setting/group_ratio.go, setting/auto_group.go, model/option.go, .planning/phases/05-group-strategy-settings/05-CONTEXT.md</read_first>
  <action>Create a new backend settings file `setting/operation_setting/group_strategy.go` that defines the persisted JSON shape for `group_strategy_settings` using a concrete Go struct and helper functions. The shape must support `enabled`, `stream_retry_first_byte_budget_seconds`, `retry_times`, `timeout_http_status`, and `timeout_error_message` per group key. Implement helpers that: parse raw option JSON using `common.UnmarshalJsonStr`, validate positive budget seconds, validate retry times as non-negative, validate HTTP status in the 100-599 range, and resolve strategy by current group string with explicit fallback "not configured" semantics. Add tests in `setting/operation_setting/group_strategy_test.go` for valid config, invalid status, invalid negative values, disabled entries, and missing-group fallback.</action>
  <verify>go test ./setting/operation_setting -run 'TestGroupStrategy'</verify>
  <acceptance_criteria>
    - `setting/operation_setting/group_strategy.go` contains `type GroupStrategy` and `type GroupStrategySettings`
    - `setting/operation_setting/group_strategy.go` contains `func ValidateGroupStrategySettings`
    - `setting/operation_setting/group_strategy.go` contains `func ResolveGroupStrategy`
    - `setting/operation_setting/group_strategy_test.go` exits 0 under `go test ./setting/operation_setting -run 'TestGroupStrategy'`
  </acceptance_criteria>
  <done>Validated per-group strategy model and fallback helpers exist with passing focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Register the option key and backend validation path</name>
  <files>model/option.go, controller/option.go, web/default/src/features/system-settings/types.ts</files>
  <read_first>model/option.go, controller/option.go, web/default/src/features/system-settings/types.ts, setting/operation_setting/group_strategy.go</read_first>
  <action>Expose the new option key `group_strategy_settings` through the option map initialization path in `model/option.go` with a stable serialized default such as `{}`. In `controller/option.go`, add a dedicated validation branch for `group_strategy_settings` that calls the new backend validator before persistence and returns a human-readable message on invalid shape. Extend `web/default/src/features/system-settings/types.ts` so Operations Settings can load and retain the raw serialized group strategy payload in a typed field, using the exact key string `group_strategy_settings`.</action>
  <verify>go test ./controller ./model/... ./setting/operation_setting -run 'TestGroupStrategy|Test.*Option' || true</verify>
  <acceptance_criteria>
    - `model/option.go` contains `common.OptionMap["group_strategy_settings"]`
    - `controller/option.go` contains a `case "group_strategy_settings":`
    - `web/default/src/features/system-settings/types.ts` contains `'group_strategy_settings': string`
  </acceptance_criteria>
  <done>The new option key is visible to the existing option pipeline and rejected when malformed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./setting/operation_setting`
- [ ] `go test ./controller ./model/...` passes for touched packages or targeted new tests pass
- [ ] Option validation path rejects malformed group strategy payloads and accepts valid ones
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend has one canonical group strategy shape and one canonical validation path
</success_criteria>

<output>
After completion, create `.planning/phases/05-group-strategy-settings/05-01-SUMMARY.md`
</output>
