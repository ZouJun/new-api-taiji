---
phase: 05-group-strategy-settings
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [web/default/src/features/system-settings/operations/section-registry.tsx, web/default/src/features/system-settings/operations/index.tsx, web/default/src/features/system-settings/types.ts, web/default/src/features/system-settings/operations/strategy-settings-section.tsx, web/default/src/i18n/locales/en.json, web/default/src/i18n/locales/zh.json, web/default/src/i18n/locales/fr.json, web/default/src/i18n/locales/ru.json, web/default/src/i18n/locales/ja.json, web/default/src/i18n/locales/vi.json]
autonomous: true
requirements: [GSET-01, GSET-02, GSET-03, GSET-05, GSET-07]
user_setup: []
must_haves:
  truths:
    - Operations Settings shows a Strategy Settings section immediately after System Behavior.
    - Operators can edit per-group strategy rows with explicit fallback and precedence guidance.
  artifacts:
    - web/default/src/features/system-settings/operations/strategy-settings-section.tsx
    - updated locale files for new operator-facing copy
  key_links:
    - operations/section-registry.tsx registers the new section in the required order
    - strategy-settings-section.tsx reads and writes the exact option key group_strategy_settings
---

<objective>
Add the operator-facing Strategy Settings UI and supporting copy.

Purpose: Make the Phase 5 backend behavior configurable and understandable to operators, with explicit fallback, precedence, and usage guidance.
Output: A new Operations Settings section, typed defaults, structured group-row editor, and translated operator-facing copy.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-group-strategy-settings/05-CONTEXT.md
@.planning/phases/05-group-strategy-settings/05-UI-SPEC.md
@.planning/phases/05-group-strategy-settings/05-RESEARCH.md
@web/default/src/features/system-settings/operations/section-registry.tsx
@web/default/src/features/system-settings/operations/index.tsx
@web/default/src/features/system-settings/general/system-behavior-section.tsx
@web/default/src/features/system-settings/models/group-ratio-visual-editor.tsx
@web/default/src/features/system-settings/hooks/use-update-option.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Strategy Settings section and typed operations defaults</name>
  <files>web/default/src/features/system-settings/operations/section-registry.tsx, web/default/src/features/system-settings/operations/index.tsx, web/default/src/features/system-settings/types.ts, web/default/src/features/system-settings/operations/strategy-settings-section.tsx</files>
  <read_first>web/default/src/features/system-settings/operations/section-registry.tsx, web/default/src/features/system-settings/operations/index.tsx, web/default/src/features/system-settings/general/system-behavior-section.tsx, web/default/src/features/system-settings/models/group-ratio-visual-editor.tsx, .planning/phases/05-group-strategy-settings/05-UI-SPEC.md</read_first>
  <action>Create `web/default/src/features/system-settings/operations/strategy-settings-section.tsx` as a structured editor for `group_strategy_settings`. It must render the section immediately after System Behavior in `operations/section-registry.tsx`, use the existing settings page save pattern, and load rows from the keys of `GroupRatio`. Each row must expose: enabled switch, stream retry first-byte budget in seconds, retry times, timeout HTTP status, and timeout error message. It must also show explicit fallback text `Uses global retry and timeout behavior` for groups without active strategy. Extend `OperationsSettings` typing and defaults in `types.ts` and `operations/index.tsx` so the raw `group_strategy_settings` option participates in settings loading.</action>
  <verify>cd web/default && bun run typecheck</verify>
  <acceptance_criteria>
    - `web/default/src/features/system-settings/operations/section-registry.tsx` contains an entry for `strategy`
    - the `strategy` entry appears after `behavior` and before `monitoring`
    - `web/default/src/features/system-settings/operations/strategy-settings-section.tsx` contains `group_strategy_settings`
    - `cd web/default && bun run typecheck` exits 0
  </acceptance_criteria>
  <done>The new Strategy Settings section is registered, typed, and editable inside Operations Settings.</done>
</task>

<task type="auto">
  <name>Task 2: Add operator guidance copy and localized strings</name>
  <files>web/default/src/features/system-settings/operations/strategy-settings-section.tsx, web/default/src/i18n/locales/en.json, web/default/src/i18n/locales/zh.json, web/default/src/i18n/locales/fr.json, web/default/src/i18n/locales/ru.json, web/default/src/i18n/locales/ja.json, web/default/src/i18n/locales/vi.json</files>
  <read_first>.planning/phases/05-group-strategy-settings/05-UI-SPEC.md, web/default/src/i18n/locales/en.json, web/default/src/features/system-settings/general/system-behavior-section.tsx, AGENTS.md</read_first>
  <action>Add all operator-facing copy required by `05-UI-SPEC.md`, including the section intro, precedence note, per-field descriptions, fallback explanation, and concrete examples for `default` and `vip` usage. Internationalize every new user-facing string and update all supported locale files (`en`, `zh`, `fr`, `ru`, `ja`, `vi`). Ensure the section clearly explains: group retry overrides global Retry Times, AWS SDK retry is separate, and stream retry budget measures total first-byte wait across retries rather than full stream duration.</action>
  <verify>cd web/default && bun run i18n:sync && bun run typecheck</verify>
  <acceptance_criteria>
    - `strategy-settings-section.tsx` contains the fallback string key for `Uses global retry and timeout behavior`
    - each locale file contains entries for `Strategy Settings` and `Uses global retry and timeout behavior`
    - `cd web/default && bun run i18n:sync && bun run typecheck` exits 0
  </acceptance_criteria>
  <done>Operators get explicit, localized guidance for precedence, fallback, and usage scenarios.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd web/default && bun run i18n:sync`
- [ ] `cd web/default && bun run typecheck`
- [ ] Strategy section order matches UI-SPEC and fallback guidance is visible in code
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The UI exposes structured group strategy editing with operator-first explanations
</success_criteria>

<output>
After completion, create `.planning/phases/05-group-strategy-settings/05-03-SUMMARY.md`
</output>
