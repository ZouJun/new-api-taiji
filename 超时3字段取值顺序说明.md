# 超时 3 字段取值顺序说明

本文只整理当前 Phase 1 已保留并实际生效的 3 个超时字段：

1. `channel.setting.non_stream_timeout_seconds`
2. `channel.setting.stream_first_byte_timeout_seconds`
3. `channel.setting.aws_invoke_timeout_seconds`

对应核心代码：

- [relay/common/timeout.go](/Users/zf/Project/new-api/relay/common/timeout.go)
- [relay/channel/aws/relay-aws.go](/Users/zf/Project/new-api/relay/channel/aws/relay-aws.go)

---

## 1. 非流式总超时

字段名：

- `channel.setting.non_stream_timeout_seconds`

取值顺序：

1. `channel.setting.non_stream_timeout_seconds`
2. `RELAY_DEFAULT_NON_STREAM_TIMEOUT`
3. `RELAY_TIMEOUT`
4. 都没有则返回 `0`

说明：

- 这是通用非流式超时
- 适用于普通非流式请求
- AWS 的非流式 HTTP Client 超时现在也复用这条链路
- 当结果为 `0` 时，表示这层不额外启用超时

---

## 2. 流式首包超时

字段名：

- `channel.setting.stream_first_byte_timeout_seconds`

取值顺序：

1. `channel.setting.stream_first_byte_timeout_seconds`
2. `RELAY_DEFAULT_STREAM_FIRST_BYTE_TIMEOUT`
3. `RELAY_TIMEOUT`
4. 都没有则返回 `0`

说明：

- 这不是整条流总超时
- 这层只控制“等待首个上游事件 / 首包”能等多久
- 首包一旦回来，这层超时控制就停止
- AWS 流式请求现在也复用这条链路
- 当结果为 `0` 时，表示不启用首包等待超时

---

## 3. AWS 非流式 Invoke 超时

字段名：

- `channel.setting.aws_invoke_timeout_seconds`

取值顺序：

1. `channel.setting.aws_invoke_timeout_seconds`
2. `AWS_INVOKE_TIMEOUT_SECONDS`
3. 回退到普通非流式超时链：
   - `RELAY_DEFAULT_NON_STREAM_TIMEOUT`
   - `RELAY_TIMEOUT`
4. 都没有则返回 `0`

说明：

- 这层只作用于 AWS 非流式 SDK `InvokeModel`
- 本质是 AWS 调用时 `context.WithTimeout(...)` 那层超时
- 不作用于 AWS 流式请求
- 当结果为 `0` 时，表示这层不额外启用 invoke 超时

---

## 4. 一句话总结

- 普通非流式：`channel.setting.non_stream_timeout_seconds -> RELAY_DEFAULT_NON_STREAM_TIMEOUT -> RELAY_TIMEOUT`
- 普通流式首包：`channel.setting.stream_first_byte_timeout_seconds -> RELAY_DEFAULT_STREAM_FIRST_BYTE_TIMEOUT -> RELAY_TIMEOUT`
- AWS 非流式 Invoke：`channel.setting.aws_invoke_timeout_seconds -> AWS_INVOKE_TIMEOUT_SECONDS -> 普通非流式链路`

---

## 5. 当前设计边界

当前已经移除的字段：

- `aws_http_client_non_stream_timeout_seconds`
- `aws_http_client_stream_first_byte_timeout_seconds`

也就是说，AWS 不再单独维护这两个 HTTP 超时配置，当前统一规则是：

- AWS 非流式 HTTP Client 超时，复用通用非流式超时
- AWS 流式首包等待超时，复用通用流式首包超时
- AWS 单独保留一个 `aws_invoke_timeout_seconds`，只控制非流式 SDK Invoke
